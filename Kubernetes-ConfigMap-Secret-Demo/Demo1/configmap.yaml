# Pod using this configmap: pod-cmd.yaml in same directory
# Config secret
apiVersion: v1
kind: ConfigMap
metadata:
  name: log-config
  namespace: default
data:
  log.level: WARNING
  log.location: LOCAL